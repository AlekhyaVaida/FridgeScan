// API Configuration
// Prefer setting EXPO_PUBLIC_API_URL in a local .env file so IPs are not committed.
export const API_BASE_URL =
  process.env.EXPO_PUBLIC_API_URL ?? 'http://localhost:8000/api/v1';

